# Allowed: apps/desktop is the one place the UI toolkit may be linked.
[package]
name = "nysia-desktop"

[dependencies]
nysia-core = { path = "../../../crates/nysia-core" }
tauri = { workspace = true }

[build-dependencies]
tauri-build = { workspace = true }
