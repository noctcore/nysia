// The module the rules are about. A stub, but a real file: the glob rules match patterns
// against the tree rather than reading extensions out of them, so a glob can only be judged
// against files that exist.
export const StoreContext = { closeTab: async (_key: string): Promise<void> => {} };
